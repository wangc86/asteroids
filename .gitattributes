# Store everything as LF in git and check it out as LF too (no CRLF on Windows).
# If an external tool writes CRLF, it is normalised back on commit, so we never
# see a whole file show up as a spurious diff.
* text=auto eol=lf
